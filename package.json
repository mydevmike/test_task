{
  "name": "demoqa-aqa-tests",
  "version": "1.0.0",
  "private": true,
  "description": "AQA test assignment: Playwright UI and API tests for DemoQA",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:ui-suite": "playwright test tests/ui",
    "test:api": "playwright test tests/api",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^22.15.0",
    "typescript": "^5.8.0"
  }
}
